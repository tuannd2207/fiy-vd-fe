import { ConvertToMbPipe } from './convert-to-mb.pipe';

describe('ConvertToMbPipePipe', () => {
    it('create an instance', () => {
        const pipe = new ConvertToMbPipe();
        expect(pipe).toBeTruthy();
    });
});
