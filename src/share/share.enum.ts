export enum HashTag {
    love = '#love',
    action = '#action'
}

export enum Actors {
    hashagi = 'hashagi',
    kagawa = 'kagawa'
}

export enum Category {
    love = 'love',
    action = 'action',
    old = 'old'
}
